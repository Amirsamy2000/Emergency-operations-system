import 'package:flutter/material.dart';
class reportmedicine extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
   return Scaffold(

     appBar: AppBar(
       title: Text(' Medication  Reports '),
       centerTitle: true,
     ),
     body: Container(),
   );
  }

}